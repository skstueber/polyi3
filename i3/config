# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# Disable screen blanking
exec --no-startup-id xset s off
# Disable monitor power-saving features
exec --no-startup-id xset -dpms

exec_always --no-startup-id setxkbmap -layout us,de -option 'grp:alt_space_toggle'


# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# Autotiling:
exec_always --no-startup-id /home/skstueber/.config/i3/scripts/autotiling-1.9.3/autotiling/main.py -w 1 2 3 4 5

# Idle screen:
# exec_always --no-startup-id ~/.config/i3/scripts/idle_exec

# Wallpaper:
exec feh --bg-fill /home/skstueber/Pictures/Wallpapers/hollow_knight.jpg

# Can hit the workspace you're in to go to last workspace
workspace_auto_back_and_forth yes

focus_follows_mouse no

# When there is only one window there are no need for borders
hide_edge_borders smart

# Diable wrapping focus on screen so that you can move your mouse to the focused window
focus_wrapping yes

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
# old defaults without a max:
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym Prior exec --no-startup-id pulsemixer --unmute --change-volume +5 --max-volume 100 #increase sound volume
bindsym Next exec --no-startup-id pulsemixer --unmute --change-volume -5 --max-volume 100 #decrease sound volume

bindsym Shift+Prior exec --no-startup-id pulsemixer --unmute --change-volume +5 --max-volume 150 #increase sound volume
bindsym Shift+Next exec --no-startup-id pulsemixer --unmute --change-volume -5 --max-volume 150 #decrease sound volume

bindsym Insert exec --no-startup-id pulsemixer --toggle-mute && $refresh_i3status

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec st ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# floating terminal
bindsym $mod+Shift+Return exec st -c "floating" ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# notes binding
bindsym $mod+n exec --no-startup-id ~/.config/i3/scripts/notes.sh

# kill process quick bind
bindsym $mod+p exec --no-startup-id ~/.config/i3/scripts/kill.sh

# killall quick bind
bindsym $mod+Shift+p exec --no-startup-id ~/.config/i3/scripts/killall.sh 

# copy histoty quick bind
bindsym $mod+c exec copy add

# select history quick bind
bindsym $mod+v exec copy sel

# clear copy history
bindsym $mod+Shift+c exec copy clear

for_window [class="floating"] floating enable
for_window [class="floating"] resize set 1200 700
for_window [class="floating"] move position center

# i3lock
bindsym $mod+Control+l exec ~/.config/i3/scripts/lock.sh

# Exit i3 (logs you out of your X session)
bindsym $mod+Control+Shift+e exec --no-startup-id ~/.config/i3/scripts/dmenu_prompt "Please close all processes before exiting. Are you absolutely certain you want to exit?" "i3-msg exit"

# kill focused window
bindsym $mod+q kill ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# sleep
bindsym $mod+Control+Shift+s exec --no-startup-id ~/.config/i3/scripts/dmenu_prompt "Are you absolutely certain you want to sleep?" "systemctl suspend"

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -fn 'FireCode-14' -nb '#282a36' -sf '#8be9fd' -sb '#44475a' -nf '#bd93f9'
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+j focus down ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+k focus up ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+l focus right ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Down focus down ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Up focus up ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Right focus right ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# move focused window
bindsym $mod+Shift+h move left ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+j move down ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+k move up ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+l move right ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+Down move down ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+Up move up ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
bindsym $mod+Shift+Right move right ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused

# split in horizontal orientation
bindsym $mod+o split h

# split in vertical orientation
bindsym $mod+b split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+e layout toggle split

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1 "
set $ws2 "2 󰿈"
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5 "
set $ws6 "6 "
set $ws7 "7 "
set $ws8 "8 "
set $ws9 "9 "
set $ws10 "10 "

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Workflow: certain apps on certain workspaces (Firefox and terminal on any):
assign [class="Piper"] $ws1
assign [class="jetbrains-clion"] $ws3
assign [class="jetbrains-pycharm"] $ws3
assign [class="jetbrains-idea"] $ws3
assign [class="zoom"] $ws6
assign [class="steam"] $ws9
assign [class="chocolate-doom"] $ws9

#
# WINDOW COLORS
#

set $bg_color        #02fff6
set $inactive        #000000
set $inactive_text   #d3d3d3
set $text            #000000
set $urgent_bg       #fddc65
set $indicator       #ec33df


# class                 border      backgr.     text             indicator       child_border
client.focused          $bg_color   $bg_color   $text            $indicator      $bg_color
client.focused_inactive $inactive   $inactive   $inactive_text   $inactive       $inactive
client.unfocused        $inactive   $inactive   $inactive_text   $inactive       $inactive
client.urgent           $urgent_bg  $urgent_bg  $text            $indicator      $urgent_bg

# getting rid of the title bar of windows
default_border pixel 2  

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Control+Shift+q exec ~/.config/i3/scripts/dmenu_prompt "Shuting down will kill all processes and applications. Are you absolutely certain you want to shutdown?" "shutdown -P now"

bindsym $mod+Control+Shift+r exec ~/.config/i3/scripts/dmenu_prompt "Are you absolutely certain you want to reboot?" "reboot" 

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available) (commented out for polybar instead)
#bar {
# status_command i3status
#}

# polybar:
exec_always ~/.config/polybar/launch.sh &

# open firefox
bindsym $mod+f exec firefox ; exec --no-startup-id ~/.config/i3/scripts/move-cursor-to-focused
